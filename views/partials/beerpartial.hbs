<div id="algo2">
    <img class="imagen" src={{this.image_url}} alt="">
    <div id="centrado">
        <h1>{{this.name}}</h1>
        <p>{{this.description}}</p>
        <h3>{{this.tagline}}</h3>
    </div>
</div>
